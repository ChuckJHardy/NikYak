<%= form_for(nik) do |f| %>
  <% if nik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nik.errors.count, "error") %> prohibited this yak from being saved:</h2>

      <ul>
        <% nik.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :parent_id %>
  <div class="field">
    <h3>What Now?</h3>
    <%= f.text_area :body, :class => "nik_fields" %>
  </div>
  <%= f.submit :class => "btn-style btn-main" %>
<% end %>