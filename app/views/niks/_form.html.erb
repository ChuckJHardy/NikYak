<%= form_for(@nik) do |f| %>
  <% if @nik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nik.errors.count, "error") %> prohibited this nik from being saved:</h2>

      <ul>
      <% @nik.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <label><%= f.label :title %></label>
    <%= f.text_field :title, :class => "nik_fields" %>
  </div>
  <div class="form-group">
    <label><%= f.label :body %></label>
    <%= f.text_area :body, :class => "nik_fields" %>
  </div>
  <div class="form-group">
    <label><%= f.label :limit %></label>
    <%= f.number_field :limit, :class => "nik_fields" %>
  </div>
  <div class="actions">
    <%= f.submit :class => "btn-style btn-main" %>
  </div>
<% end %>