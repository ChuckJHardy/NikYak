<%= form_for(nik) do |f| %>
  <% if nik.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(nik.errors.count, "error") %> prohibited this nik from being saved:</h2>

      <ul>
      <% nik.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :parent_id %>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :limit %><br>
    <%= f.number_field :limit %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
